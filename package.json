{
  "name": "kirk-amos-precalc",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "slidev curriculum/ch04-trigonometry/4.2-unit-circle.md --open",
    "build": "node build.mjs",
    "preview": "npm run build && npx -y serve dist"
  },
  "devDependencies": {
    "@slidev/cli": "^52.15.2",
    "@slidev/theme-default": "^0.25.0"
  }
}
